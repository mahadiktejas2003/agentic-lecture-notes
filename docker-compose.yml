version: '3.8'
services:
  pipeline:
    build: .
    volumes:
      - ./lecture-input:/app/lecture-input
      - ./notes-output:/app/notes-output
      - ./agent_memory:/app/agent_memory
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
